import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  buttonText: {
    color: "white",
    fontWeight: "bold",
    textAlign: "center",
    margin: 12,
  },
  button: {
    borderColor: "white",
    borderWidth: 1,
    backgroundColor: "gold",
    borderRadius: 10,
  },
});
